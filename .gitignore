node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Base de données SQLite et données locales
/data/*
!/data/.gitkeep
*.db
*.db-shm
*.db-wal
*.db.before-*
*.db.backup-*

.eslintcache

# Lockfiles: package-lock.json IS tracked (npm is the project's package manager,
# and the Docker build needs it for `npm ci`). Other managers' lockfiles are not.
yarn.lock
pnpm-lock.yaml
bun.lockb
bun.lock

# Claude Code
.claude/

# Packaging artifacts
build/artifacts/*.tgz
build/artifacts/*.tar.gz
