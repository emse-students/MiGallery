build/
dist/
node_modules/
.svelte-kit/
.env*
*.min.*
cookies.txt

# Generated by `paraglide-js compile`, which runs on every `check` and every build: 500+ message
# modules nobody edits, plus the inlang project's own scratch files. Formatting them is not a
# style question, it is a gate that reports whether the compiler ran last.
src/lib/paraglide/
project.inlang/cache/
project.inlang/.meta.json
project.inlang/README.md
